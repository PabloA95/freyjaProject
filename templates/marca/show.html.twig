{% extends 'base.html.twig' %}

{% block title %}Marca{% endblock %}

{% block body %}
<div class="container">
  <div class="col-md-8 offset-md-2">
    <h1>Marca</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ marca.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marca_index') }}" class="btn btn-primary width7em">Volver  <i class="material-icons align-bottom">arrow_back</i></a>
    <a href="{{ path('marca_edit', {'id': marca.id}) }}" class="btn btn-success width7em">Editar  <i class="material-icons align-bottom">edit</i></a>

    {{ include('marca/_delete_form.html.twig') }}
  </div>
</div>
{% endblock %}
