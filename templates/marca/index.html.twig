{% extends 'base.html.twig' %}

{% block title %}Marcas{% endblock %}

{% block body %}
<div class="container">
  <div class="col-md-8 offset-md-2">
    <h1>Marcas</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for marca in marcas %}
            <tr>
                <td>{{ marca.descripcion }}</td>
                <td>
                    <a href="{{ path('marca_show', {'id': marca.id}) }}" class="btn btn-primary">Ver</a>
                    <a href="{{ path('marca_edit', {'id': marca.id}) }}" class='btn btn-info'>Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('marca_new') }}" class='btn btn-success'>Agregar marca</a>
  </div>
</div>
{% endblock %}
