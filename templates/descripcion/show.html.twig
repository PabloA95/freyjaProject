{% extends 'base.html.twig' %}

{% block title %}Descripcion{% endblock %}

{% block body %}
    <h1>Descripcion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ descripcion.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ descripcion.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('descripcion_index') }}">back to list</a>

    <a href="{{ path('descripcion_edit', {'id': descripcion.id}) }}">edit</a>

    {{ include('descripcion/_delete_form.html.twig') }}
{% endblock %}
