{% extends 'base.html.twig' %}

{% block title %}Descripciones{% endblock %}

{% block body %}
<div class="container">
  <div class="col-md-8 offset-md-2">
    <h1>Descripciones</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for descripcion in descripcions %}
            <tr>
                <td>{{ descripcion.id }}</td>
                <td>{{ descripcion.descripcion }}</td>
                <td>
                    <a href="{{ path('descripcion_show', {'id': descripcion.id}) }}" class="btn btn-primary">Ver</a>
                    <a href="{{ path('descripcion_edit', {'id': descripcion.id}) }}" class='btn btn-info'>Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('descripcion_new') }}" class='btn btn-success'>Agregar descripcion</a>
  </div>
</div>
{% endblock %}
