<form method="post" class="form-check form-check-inline" action="{{ path('producto_delete', {'id': producto.id}) }}" onsubmit="return confirm('¿Estas segura que deseas eliminar este producto?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-danger width7em">Borrar <i class="material-icons align-bottom">delete</i></button>
</form>
